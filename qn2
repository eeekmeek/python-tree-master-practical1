# computing the volume of a cylinder

radius = float(input("Radius of the cylinder = "))
length = float(input("Length of the cylinder = "))

area = (radius ** 2) * 3.1416 # pi to 5 s.f.
volume = area * length

print("Volume of the cylinder = {0:.2f}".format(volume))
